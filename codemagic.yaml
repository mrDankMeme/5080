workflows:
  ios-5080-release:
    name: 5080
    max_build_duration: 120
    instance_type: mac_mini_m1

    integrations:
      app_store_connect: app5080v2

    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.arn.5080base44

      vars:
        BUNDLE_ID: "com.arn.5080base44"
        XCODE_PROJECT: "5080/5080/5080.xcodeproj"
        XCODE_SCHEME: "5080"
        APP_STORE_APPLE_ID: 6762474175

      xcode: 26.2

    scripts:
      - name: Resolve Swift Package Manager dependencies
        script: |
          xcodebuild -resolvePackageDependencies \
            -project "$CM_BUILD_DIR/$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME"

      - name: Set up provisioning profiles settings on Xcode project
        script: |
          xcode-project use-profiles \
            --project "$CM_BUILD_DIR/$XCODE_PROJECT"

      - name: Increment build number (from App Store Connect)
        script: |
          cd "$CM_BUILD_DIR/$(dirname "$XCODE_PROJECT")"
          LATEST_BUILD_NUMBER=$(app-store-connect get-latest-app-store-build-number "$APP_STORE_APPLE_ID" || echo "0")
          [[ "$LATEST_BUILD_NUMBER" =~ ^[0-9]+$ ]] || LATEST_BUILD_NUMBER=0
          agvtool new-version -all $((LATEST_BUILD_NUMBER + 1))

      - name: Build IPA for App Store distribution
        script: |
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/$XCODE_PROJECT" \
            --scheme "$XCODE_SCHEME"

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.app
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM

    publishing:
      email:
        recipients:
          - hassan_emily8936@aol.com
        notify:
          success: true
          failure: true

      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false
